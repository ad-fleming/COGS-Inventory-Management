
<div class="row center-content">
    <div class="col s1 hide-on-small-only"> </div>
    <div class="col s12 m12">
      <div class="card grey darken-4 section">
        <div class="card-content white-text">
          <span class="card-title"><h2>COGS</h2></span>
          <p class="section">This your MAIN item sheet that contains all of your inventory items. You can edit any of the fields if they need updating, delete by line or
            simply gaze in wonder at the awesome things you have! Editing this sheet will not impact previous inventories, because magic.</p> 
      
        {{#with masterInventory}}
        <p>{{[0].dataValues.Inventory.dataValues.inventory_date}}</p>
        {{/with}}

        <form class="white-text logo-push">    
        
        {{#each masterInventory}}
        {{!-- <p>Item Id:</p>  --}}
        <div class="row section">
        <div class="col s4 l4">
        <h4 class="" data-attribute={{id}}> {{unit_name}} </h4> 
        <h5>{{unit_category}}</h5>
        <h5>{{unit_distributor}}</h5>
        {{!-- {{Inventory.inventory_date}} --}}
        </div>
            
            <div class="col s4 l4">
            <p>Unit Par:</p>
            <input class="input-field white-text" type="number" placeholder={{unit_par}}>
            <p>Unit Price:</p>
            <input class="input-field white-text" type="number" placeholder={{unit_price}}>
            <p>Items Per Unit:</p>
            <input class="input-field white-text" type="number" placeholder={{items_per_unit}}>
            <p>Item Count Type:</p>
            <input class="input-field white-text" type="text" placeholder={{item_count_type}}>
            <p>Item Par:</p>
            <input class="input-field white-text" type="number" placeholder={{item_count_par}}>
            </div>
            
            <div class="col s4 l4">
            <p>Unit Count:</p>
            <input class="input-field white-text" type="number" placeholder={{unit_count}}>
            <p>Item Count:</p>
            <input class="input-field white-text" type="number" placeholder={{item_count}}>
            <p>Total Value:</p>
            <input class="input-field white-text" type="number" placeholder={{total_value}}>
            <p>Inventory Id:</p>
            <input class="input-field white-text" type="number" placeholder={{InventoryId}}>
            </div>

            </div>

            {{/each}}
        </form>
    </div>
              <div class="row"> 
                  <div class="col s6 l9 section"> </div>
              <div class="col s3 l3"> 
                  <button class="ui button lime accent-2 right" type= "submit" id="updateInventory">
                    <p class="black-font">Update Inventory</p>
                    </button>
                </div>
             </div>
          </div>
          </div>

    <div class="col s2 hide-on-small-only"> </div>
  </div>
