
<div class="row center-content">
    <div class="col s1 hide-on-small-only"> </div>
    <div class="col s12 m12">
      <div class="card grey darken-4 section">
        <div class="card-content white-text">
          <span class="card-title"><h2>COGS</h2></span>
          <p class="section">The world is an uncertain place, but your inventory and sales tracking doesn’t have to be, regardless of your size. COGS is a business inventory management system that will meet you exactly where you are and deliver results!</p>
          <p class="section">Use our customizable inventory database to help your business thrive.</p>
        </div>

        <form class="white-text logo-push">    
        {{masterInventory.Inventory.inventory_date}}
        {{#each masterInventory}}
            {{!-- <p>Item Id:</p>  --}}
            <div class="row">
            <div class="col l12 s12">
            <p class="white-text" data-attribute=`{{id}}`> 
            {{unit_name}}: Unit Category: {{unit_category}}</p>
            <p>Unit Distributor:</p>
             <input class="white-text" type="number" placeholder=`{{unit_distributor}}`>
            <p>Unit Par:</p>
            <input class="white-text" type="number" placeholder=`{{unit_par}}`>
            </div>

            <div class="col s4 l4">

            <p>Unit Price:</p>
            <input class="white-text" type="number" placeholder=`{{unit_price}}`>
            <p>Items Per Unit:</p>
            <input class="white-text" type="number" placeholder=`{{items_per_unit}}`>
            <p>Item Count Type:</p>
            <input class="white-text" type="number" placeholder=`{{item_count_type}}`>
            <p>Item Par:</p>
            <input class="white-text" type="number" placeholder=`{{item_count_par}}`>
            </div>
            
            <div class="col s4 l4">
            <p>Unit Count:</p>
            <input class="white-text" type="number" placeholder=`{{unit_count}}`>
            <p>Item Count:</p>
            <input class="white-text" type="number" placeholder=`{{item_count}}`>
            <p>Total Value:</p>
            <input class="white-text" type="number" placeholder=`{{total_value}}`>
            <p>Inventory Id:</p>
            <input class="white-text" type="number" placeholder=`{{InventoryId}}`>
            </div>

            </div>

            {{/each}}
        </form>
              <div class="row"> 
                  <div class="col s6 section"> </div>
                  <div class="col s3">
                    <button class="ui button lime accent-2 right" type= "submit" id="loginButton"><p>Login</p></button>
                  </div>
              <div class="col s3"> 
                  <button class="ui button lime accent-2 right" type= "submit" id="userButton">
                    <p class="black-font">New User</p>
                    </button>
                </div>
             </div>
          </div>
          </div>

    <div class="col s2 hide-on-small-only"> </div>
  </div>
